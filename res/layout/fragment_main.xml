<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="edu.xaut.mvpdemo.MainActivity$PlaceholderFragment" >

    <EditText
        android:id="@+id/id_et_username"
        android:layout_width="500dp"
        android:layout_height="70dp"
        android:layout_marginTop="30dp"
        android:inputType="text" />
    
    <EditText
        android:id="@+id/id_et_password"
        android:layout_width="500dp"
        android:layout_height="70dp"
        android:layout_below="@id/id_et_username"
        android:layout_marginTop="30dp"
        android:inputType="numberPassword" />
    
    <Button
        android:id="@+id/id_btn_clear"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:layout_below="@id/id_et_password"
        android:layout_marginTop="30dp"
        android:text="@string/cancel_str" />
    
    <Button
        android:id="@+id/id_btn_login"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:layout_alignBottom="@id/id_btn_clear"
        android:layout_toRightOf="@id/id_btn_clear"
        android:layout_marginLeft="30dp"
        android:text="@string/login_str" />
    
    <ProgressBar
        android:id="@+id/id_pb_loading"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_below="@id/id_btn_login"
        android:layout_marginTop="30dp" />

</RelativeLayout>
